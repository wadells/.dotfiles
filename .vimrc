" javins vim settings <walt.javins.net>
set nocompatible

syntax on	" syntax hilighting on
set incsearch	" incremental search
set ignorecase	" ignore case when searching
set autoindent	" always auto indent to level of previous line
set smartindent " when writing code, guess about indentation levels
set expandtab   " expand wierd tab syntax
set showmatch   " show matching braces, parentheses, etc
set cursorline	" show cursor line
set number	" show line numbers
colorscheme ir_black